// Code generated by ent, DO NOT EDIT.

package ent

import (
	"beetle/app/im/internal/data/ent/loadrecord"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// LoadRecord is the model entity for the LoadRecord schema.
type LoadRecord struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 用户ID
	UserID int `json:"user_id,omitempty"`
	// 设备号
	DeviceID     uuid.UUID `json:"device_id,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*LoadRecord) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case loadrecord.FieldID, loadrecord.FieldUserID:
			values[i] = new(sql.NullInt64)
		case loadrecord.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case loadrecord.FieldDeviceID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the LoadRecord fields.
func (lr *LoadRecord) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case loadrecord.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			lr.ID = int(value.Int64)
		case loadrecord.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				lr.CreatedAt = value.Time
			}
		case loadrecord.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				lr.UserID = int(value.Int64)
			}
		case loadrecord.FieldDeviceID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field device_id", values[i])
			} else if value != nil {
				lr.DeviceID = *value
			}
		default:
			lr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the LoadRecord.
// This includes values selected through modifiers, order, etc.
func (lr *LoadRecord) Value(name string) (ent.Value, error) {
	return lr.selectValues.Get(name)
}

// Update returns a builder for updating this LoadRecord.
// Note that you need to call LoadRecord.Unwrap() before calling this method if this LoadRecord
// was returned from a transaction, and the transaction was committed or rolled back.
func (lr *LoadRecord) Update() *LoadRecordUpdateOne {
	return NewLoadRecordClient(lr.config).UpdateOne(lr)
}

// Unwrap unwraps the LoadRecord entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (lr *LoadRecord) Unwrap() *LoadRecord {
	_tx, ok := lr.config.driver.(*txDriver)
	if !ok {
		panic("ent: LoadRecord is not a transactional entity")
	}
	lr.config.driver = _tx.drv
	return lr
}

// String implements the fmt.Stringer.
func (lr *LoadRecord) String() string {
	var builder strings.Builder
	builder.WriteString("LoadRecord(")
	builder.WriteString(fmt.Sprintf("id=%v, ", lr.ID))
	builder.WriteString("created_at=")
	builder.WriteString(lr.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", lr.UserID))
	builder.WriteString(", ")
	builder.WriteString("device_id=")
	builder.WriteString(fmt.Sprintf("%v", lr.DeviceID))
	builder.WriteByte(')')
	return builder.String()
}

// LoadRecords is a parsable slice of LoadRecord.
type LoadRecords []*LoadRecord
